export const enum UnoParams {
  /**
   * Number of cards the user starts with
   */
  InitialCardsDrawn = 7,

  /**
   * Number of cards a user draws if nothing in the hand can be played
   */
  CardsDrawnIfNothing = 1,


  // add things as needed
}
